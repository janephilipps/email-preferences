<p id="notice"><%= notice %></p>

<p>
  <strong>Nonce:</strong>
  <%= @token.nonce %>
</p>

<p>
  <strong>User:</strong>
  <%= @token.user %>
</p>

<%= link_to 'Edit', edit_token_path(@token) %> |
<%= link_to 'Back', tokens_path %>
